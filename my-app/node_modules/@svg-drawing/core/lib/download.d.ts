export declare const mimeTypeMap: {
    readonly png: "image/png";
    readonly jpg: "image/jpeg";
    readonly svg: "image/svg+xml";
};
export declare const downloadBlob: ({ data, extension, filename, }: {
    data: string;
    extension: keyof typeof mimeTypeMap;
    filename: string;
}) => void;
export declare class Download {
    private el;
    private download;
    constructor(el: SVGSVGElement, _download?: typeof downloadBlob);
    svg(filename: string): void;
    png(filename: string): void;
    jpg(filename: string): void;
    toBase64(): string;
    private renderCanvas;
}
