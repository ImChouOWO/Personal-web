import type { DrawingOption, PathClass, ResizeCallback, SvgObject, EventPoint } from './types';
/**
 * ### Setup SvgDrawing
 *
 * ```ts
 * import { SvgDrawing } from '@svg-drawing/core'
 *
 * const el = document.getElementById('draw-area')
 * new SvgDrawing(el)
 * ```
 *
 * ### Set draw options.
 *
 * ```ts
 * // It is default value
 * const options = {
 *   penColor: '#000',
 *   penWidth: 1,
 *   curve: true,
 *   close: false,
 *   delay: 0,
 *   fill: 'none',
 *   background: undefined,
 * }
 *
 * new SvgDrawing(el, options)
 * ```
 */
export declare class SvgDrawing {
    private svg;
    private drawing;
    private factory;
    private handler;
    private renderer;
    constructor(el: HTMLElement, { curve, close, delay, penColor, penWidth, fill, background, }?: DrawingOption);
    drawStart(): void;
    drawMove(po: EventPoint): void;
    drawEnd(): void;
    toJson(): SvgObject;
    changeCurve(curve: boolean): void;
    changeClose(close: boolean): void;
    clear(): PathClass[];
    undo(): PathClass | undefined;
    private update;
    resize({ width, height }: Parameters<ResizeCallback>[0]): void;
}
