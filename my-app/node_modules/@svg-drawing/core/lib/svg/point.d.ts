import type { VectorObject, PointClass, PointObject } from '../types';
export declare class Point implements PointClass {
    private _x;
    private _y;
    constructor(x: number, y: number);
    get x(): number;
    get y(): number;
    scale(r: number): Point;
    scaleX(r: number): Point;
    scaleY(r: number): Point;
    add(p: PointObject): Point;
    sub(p: PointObject): Point;
    clone(): Point;
    toJson(): {
        x: number;
        y: number;
    };
    /**
     * ### Calculate coordinate from vector.
     *
     * ```ts
     * const point = Point.fromVector({ value: 1, 0 })
     *
     * console.log(point.x) // 0
     * console.log(point.y) // 1
     * ```
     */
    static fromVector({ value, angle }: VectorObject): Point;
}
