import type { SvgOption, PathClass, SvgClass } from '../types';
export declare class Svg implements SvgClass {
    paths: PathClass[];
    width: number;
    height: number;
    background?: string;
    constructor({ width, height, background }: SvgOption);
    resize({ width, height }: {
        width: number;
        height: number;
    }): void;
    private scale;
    addPath(pa: PathClass | PathClass[]): this;
    getPath(key: string): PathClass | undefined;
    updatePath(path: PathClass): this;
    deletePath(deletePath: PathClass): this;
    clonePaths(): PathClass[];
    toJson(): {
        width: number;
        height: number;
        background: string | undefined;
        paths: import("../types").PathObject[];
    };
    /**
     * Copy resized paths.
     *
     * @example
     *
     * ```ts
     * const drawSvg = new Svg()
     * const animateSvg = new Svg().copy(drawSvg)
     * ```
     */
    copy(svg: SvgClass): this;
    clone(): Svg;
}
