import type { EventPoint, CommandClass, PointObject, ConvertOption, CreateCommand } from '../types';
export declare const createLineCommands: CreateCommand;
interface GenerateCommandsConverter {
    create: CreateCommand;
}
export declare class BezierCurve implements GenerateCommandsConverter {
    ratio: number;
    constructor({ ratio }?: ConvertOption);
    genCommand(p1: PointObject, p2: PointObject, p3: PointObject, p4: PointObject): CommandClass;
    create(p: EventPoint[]): CommandClass[];
}
export declare const closeCommands: (commands: CommandClass[]) => CommandClass[];
export {};
