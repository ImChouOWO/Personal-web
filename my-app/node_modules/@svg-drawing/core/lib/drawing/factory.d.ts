import type { CreateCommand, DrawFactory, PathAttributes, PathClass } from '../types';
export declare class BasicDrawFactory implements DrawFactory {
    private attrs;
    private opts;
    constructor(attrs: PathAttributes, opts?: {
        curve?: boolean;
        close?: boolean;
    });
    createPath(): PathClass;
    get createCommand(): CreateCommand;
    setPathAttributes(attrs: PathAttributes): void;
    updatePathAttributes(attrs: PathAttributes): void;
    changeCurve(curve: boolean): void;
    changeClose(curve: boolean): void;
    get curveAttribute(): {
        strokeLinecap: string;
        strokeLinejoin: string;
    };
}
