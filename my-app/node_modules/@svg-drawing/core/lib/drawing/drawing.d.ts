import type { DrawingClass } from '..';
import type { EventPoint, DrawFactory, SvgClass } from '../types';
/**
 * ### Basic Drawing usage.
 *
 * ```ts
 * import {
 *   Svg,
 *   Drawing,
 *   PenHandler,
 *   Renderer,
 *   BasicDrawFactory,
 * } from '@svg-drawing/core'
 *
 * const el = document.getElementById('draw')
 * const { width, height } = el.getBoundingClientRect()
 *
 * new Drawing(
 *   new Svg({ width, height }),
 *   new BasicDrawFactory({ stroke: '#000' }),
 *   new PenHandler(el),
 *   new Renderer(el).update
 * )
 * ```
 */
export declare class Drawing implements DrawingClass {
    svg: SvgClass;
    pathFactory: DrawFactory;
    private update;
    private _drawPath;
    private _drawPoints;
    constructor(svg: SvgClass, pathFactory: DrawFactory, update: (svg: SvgClass) => void);
    start(): void;
    dot(po: EventPoint): void;
    end(): void;
}
