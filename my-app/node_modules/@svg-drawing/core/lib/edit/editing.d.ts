import type { PointObject, PathAttributes, SelectIndex, FixedType } from '../types';
import type { EditSvg } from './editSvg';
export declare class Editing {
    editSvg: EditSvg;
    /** Callback function that refreshes the screen. */
    updater: (eSvg: EditSvg) => void;
    constructor(editSvg: EditSvg, 
    /** Callback function that refreshes the screen. */
    updater?: (eSvg: EditSvg) => void);
    /** Set the callback function to update the screen */
    setupUpdater(upd: (eSvg: EditSvg) => void): void;
    /** Clear selected status and update screen. */
    cancel(): void;
    /** Select edit path and update screen. */
    select(index: SelectIndex, multipleSelect?: boolean): void;
    /** Delete path and update screen. */
    deletePaths(): void;
    /** Change attributes and update screen. */
    changeAttributes(attrs: PathAttributes): void;
    translatePreview(po: PointObject): void;
    translate(po: PointObject): void;
    private translateEditSvg;
    resize(fixedType: FixedType, po: PointObject): void;
    resizePreview(fixedType: FixedType, po: PointObject): void;
    private resizeEditSvg;
}
