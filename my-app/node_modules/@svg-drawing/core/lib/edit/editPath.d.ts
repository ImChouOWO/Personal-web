import type { EditPathObject, PathClass } from '../types';
import type { PathSelector } from './pathSelector';
export declare class EditPath {
    private path;
    private selector;
    constructor(path: PathClass, selector: PathSelector);
    toJson(): EditPathObject;
    private get vertex();
}
