import type { ResizeBoundingBoxBase } from '../types';
import type { Editing } from './editing';
export declare class ResizePathHandler {
    editing: Editing;
    private resizeBoundingBoxBase;
    constructor(editing: Editing);
    private getResizeMovePoint;
    private handleResizePathPreview;
    private handleResizePathEnd;
    start(base: ResizeBoundingBoxBase): void;
    end(): void;
}
