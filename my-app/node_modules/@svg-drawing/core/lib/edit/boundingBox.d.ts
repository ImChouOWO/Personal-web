import type { PointObject, FixedType, PathClass } from '../types';
export declare class BoundingBox {
    private paths;
    constructor(paths: PathClass[]);
    private get points();
    private get pointsX();
    private get pointsY();
    get min(): PointObject;
    get max(): PointObject;
    get width(): number;
    get height(): number;
    get vertex(): Record<FixedType, PointObject>;
    resizeParams(fixedType: FixedType, movePoint: PointObject): {
        scale: PointObject;
        move: PointObject;
    };
}
