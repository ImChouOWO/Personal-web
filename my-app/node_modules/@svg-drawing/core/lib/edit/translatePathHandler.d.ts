import type { PointObject } from '../types';
import type { Editing } from './editing';
export declare class TranslatePathHandler {
    editing: Editing;
    private basePoint;
    constructor(editing: Editing);
    private handleTranslatePreview;
    private handleTranslateEnd;
    private getTranslatePoint;
    start(po: PointObject): void;
    end(): void;
}
