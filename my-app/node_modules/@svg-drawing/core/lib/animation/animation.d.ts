import type { PathClass, AnimationOption, FrameAnimation, AnimationObject } from '../types';
export declare class Animation {
    ms: number;
    paths: PathClass[];
    generator?: Generator<PathClass[], void, unknown>;
    private _frame;
    private _repeatCount?;
    constructor({ ms }?: AnimationOption);
    /**
     * `frame` is the number of frames to animate `repeat` is related for
     * repeatCount of animate element attribute.
     *
     * @param {FramaAnimation} fn
     * @param {{ frame?: number; repeat?: number }} opts
     */
    setup(frame: FrameAnimation, { repeatCount, ms }?: {
        repeatCount?: number;
        ms?: number;
    }): void;
    getFramePaths(key: number): PathClass[];
    restorePaths(): PathClass[];
    private setupGenerator;
    initialize(paths: PathClass[]): this;
    toJson(): AnimationObject;
}
