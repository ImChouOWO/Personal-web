import { Animation } from './animation/animation';
import type { AnimationOption, RendererOption, ResizeCallback, SvgClass } from './types';
export declare class SvgAnimation {
    svg: SvgClass;
    animation: Animation;
    private update;
    private _stopId?;
    constructor(svg: SvgClass, animation: Animation, update: (svg: SvgClass) => void);
    stop(): void;
    restore(): void;
    start(): void;
    private _setupAnimation;
    private _updateFrame;
    private _startAnimation;
    toElement(): SVGSVGElement;
    resize({ width, height }: Parameters<ResizeCallback>[0]): void;
    static init(el: HTMLElement, { ms, background }?: AnimationOption & RendererOption): SvgAnimation;
}
