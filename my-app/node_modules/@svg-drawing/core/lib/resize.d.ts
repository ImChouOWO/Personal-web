import type { ResizeEventHandler, ResizeCallback } from './types';
/**
 * ### Resize SVG to match element resizing
 *
 * ```ts
 * const svg = new Svg()
 * const resizeHandler = new ResizeHandler(
 *   document.getElementById('draw-area')
 * )
 *
 * resizeHandler.setHandler(({ width, height }) =>
 *   svg.resize({ width, height })
 * )
 * resizeHandler.on()
 * ```
 */
export declare class ResizeHandler implements ResizeEventHandler {
    private el;
    /** Remove EventList */
    private _clearEventList;
    private resizeCallback;
    private width;
    private height;
    private threshold;
    constructor(el?: HTMLElement | null);
    get active(): boolean;
    off(): void;
    on(): void;
    setElement(el: HTMLElement): this;
    setHandler(resizeCallback: ResizeCallback): void;
    private resize;
    private handleResize;
    private _setupListener;
}
