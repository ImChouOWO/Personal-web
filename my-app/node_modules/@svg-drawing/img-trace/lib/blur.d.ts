export interface BlurOption {
    radius?: number;
    delta?: number;
}
export declare class Blur {
    radius: number;
    delta: number;
    constructor({ radius, delta }: BlurOption);
    apply(argimgd: ImageData): ImageData;
}
