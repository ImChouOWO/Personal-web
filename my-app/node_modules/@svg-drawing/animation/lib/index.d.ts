import type { PathClass, PathAttributes, FrameAnimation } from '@svg-drawing/core';
export declare class ShakeFrame implements FrameAnimation {
    private range;
    readonly loops = 10;
    constructor(range?: number);
    private randomNumber;
    animation(paths: PathClass[]): PathClass[];
}
export declare class AttributeFrame implements FrameAnimation {
    private attributesList;
    constructor(attributesList: PathAttributes[]);
    get loops(): number;
    animation(paths: PathClass[], key: number): PathClass[];
}
export declare class DrawFrame implements FrameAnimation {
    private paths;
    constructor(paths: PathClass[]);
    get loops(): number;
    animation(paths: PathClass[], key: number): PathClass[];
}
