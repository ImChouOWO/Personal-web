import type { DrawFactory, PathAttributes } from '@svg-drawing/core';
export declare const useDrawFactory: (pathAttrs: PathAttributes, { curve, close, }: {
    curve: boolean;
    close: boolean;
}) => DrawFactory;
